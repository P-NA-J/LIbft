#include "libft.h"
#include <stdarg.h>
# define TYPE_DEF
# define TYPE t_list

void	list_pushback(int n_arg, TYPE *begin, ...)
{
	va_list	arg;

	n_arg = 0;
	va_start(arg, begin);
	begin = NULL;
}

int	main()
{
	TYPE *list;
	list = NULL;
	return (0);
}
